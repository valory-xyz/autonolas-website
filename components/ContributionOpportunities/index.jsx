import React from 'react';
// import PropTypes from 'prop-types';
// import { connect } from 'react-redux';

const ContributionOpportunities = () => <>CONTRIBUTION OPPORTUNITIES</>;

export default ContributionOpportunities;

/*
ContributionOpportunities.propTypes = {
  isNavigationOpen: PropTypes.bool.isRequired,
  cofounders: PropTypes.instanceOf(Array),
  foundingTeam: PropTypes.instanceOf(Array),
  press: PropTypes.instanceOf(Array),
  blogs: PropTypes.instanceOf(Array),
};

ContributionOpportunities.defaultProps = {
  cofounders: [],
  foundingTeam: [],
  press: [],
  blogs: [],
};

const mapStateToProps = state => {
  const { isNavigationOpen } = state.navigation;
  return { isNavigationOpen };
};

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(ContributionOpportunities);
*/
